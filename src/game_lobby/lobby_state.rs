// Represents the state of the lobby.
pub struct LobbyState {}

impl LobbyState {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for LobbyState {
    fn default() -> Self {
        Self {}
    }
}
